// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://tilsonf.github.io',
  base: '/little-dream-books-landing',
  output: 'static',
});
